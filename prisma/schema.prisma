generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../public/db.db"
}

model other_test {
  id      Int     @id @default(autoincrement())
  name    String?
  message String?
}

model test {
  id      Int     @id @default(autoincrement())
  name    String?
  message String?
}
